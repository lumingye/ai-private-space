# Security / 安全说明

## 支持范围

本仓库是设计提案与参考实现，不是托管服务，也不承诺生产级安全。

## 已知边界

1. **直接 MCP 工具轨迹可能可见。** 工具名称、参数、结果、时间与错误信息可能由宿主前端展示或记录。
2. **数据库加密不保护加密前的明文通道。** 正文若先经过浏览器、宿主客户端或公开工具参数，之后加密并不能撤回已经暴露的信息。
3. **私有 gateway 只解决前端隔离。** 它不能自动防止 VPS 管理员、模型供应商、进程调试器或被攻破的服务端读取运行时明文。
4. **密语本不是密码学保护。** 固定代号和一次性替换主要用于语义遮挡和惊喜，不应用于真正敏感内容。
5. **参考代码没有完整认证与生产运维能力。** 不要直接用于凭证、法律医疗资料、第三方隐私或不可承受泄露的数据。
6. **密码学不能判断自然语言是否包含责任。** 参考代码会在缺少受信任责任审查器时拒绝新写入，但不自带通用语义分类器或普通责任账本；错误或受写入者控制的 guard 仍可能误分。

## 部署前必须检查

- 浏览器网络响应中没有私人工具参数与结果；
- 流式事件、WebSocket/SSE、控制台和错误页面不包含正文；
- 访问日志、应用日志、异常堆栈和审计日志不落明文；
- 密钥不与数据库、仓库或备份放在同一位置；
- 备份、恢复、密钥轮换和永久删除已经实际测试；
- 私人正文未进入 FTS、embedding、普通召回、画像或摘要；
- 每次新建和实质更新都经过不由本次写入者控制的责任审查器；
- 模型或客户端不能提交自己的 `private_only` 分类、普通账本引用或审查回执；
- 责任性内容的普通记录足以继续追踪责任，且不会随私人正文删除；
- guard 不记录私人正文；审查失败、回执错配与普通账本不可用时关闭失败；
- 升级前条目标为 `legacy_unreviewed`，并有独立复核流程；
- 系统故障不会被显示成 AI 的拒绝、沉默或离开。

## 报告问题

公开仓库上线后，建议启用 GitHub Security Advisories。报告安全问题时不要在公开 Issue 中附上真实正文、密钥、域名、数据库或可复现的私人数据。
