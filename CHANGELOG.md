# Changelog

## v0.6.1 — 责任锚点关闭失败补丁（2026-07-20）

- 将“不能隐藏责任”从使用守则提升为参考实现中的写入前置条件；
- 新增独立 `AccountabilityGuard` 合同，责任分类与普通账本回执不能由本次写入者通过工具参数自签；
- 未配置审查器、审查异常、回执错配或责任性内容缺少普通记录时，新写入关闭失败；
- 承诺、错误、任务、关系边界、项目变化、重大决定和安全事项必须先取得普通账本持久引用；
- 凭证、未经允许的第三方隐私和隐藏推理不能靠留引用进入私人空间；
- 责任回执随正文加密且不出现在最小信封中；旧条目继续可读，但明确标为 `legacy_unreviewed`；
- 新增完整 v0.6.1 主文档并将安全回归测试扩充为 39 项。

## v0.6 — 完整设计与参考实现安全修订（2026-07-16）

- 合并并取代 v0.3、v0.4 与 v0.5.1，形成一份可独立阅读的完整主文档；
- 保留定位、所有权、生命周期、提醒、三档隐私、无 gateway 方案、数据模型、API、成本与验收测试；
- 明确请求中的 `persona_id` 不是授权，owner 必须来自服务端认证上下文；
- 新增不可信内容、提示注入、URL 与附件沙箱边界；
- 修正密码学删除表述，区分在线副本、备份、密钥销毁与外部副本；
- 强化 gateway：私密与未分类工具事件关闭失败，公开工具显式登记，禁止事件自行降级；
- 强化参考 vault：写入前主密钥认证、旧库安全迁移、提醒元数据认证、随机 ID、严格日期与原子提醒领取；
- 收紧日志、输入限制、配置校验与错误处理；
- 扩充为 33 项安全回归测试。

## v0.5.1 — 简明综合版

- 将完整方案与工具轨迹补丁合并为可独立阅读的简明文档；
- 保留生命周期、隐私分级、前端显示、替代方案、Token 成本和验收测试；
- 明确直接 MCP 只能视为软隐私。

## v0.4 — 工具轨迹与前端兼容补丁

- 新增宿主前端可查看 MCP 参数与返回结果的威胁模型；
- 区分直接 MCP、私有 gateway 和管理员隔离；
- 增加密语本、一次性替换、轻量封存与盲盒承诺；
- 增加前端事件过滤、日志脱敏、兼容矩阵与测试要求。

## v0.3 — 完整可复现提案

- 定义 AI 私人空间的用途、所有权、数据边界与生命周期；
- 规定不进入普通召回、FTS、embedding、画像和摘要；
- 定义身份锚点、提醒、共享副本、删除与恢复；
- 提出“房间可以有秘密，但不能藏责任”。
